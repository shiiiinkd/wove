# MCP Server (Wove)

`save_curriculum` ツールを利用するには、以下の環境変数を設定してください。

- `WOVE_API_URL` : Wove API のベース URL（例: `http://localhost:8080`）
- `SUPABASE_ACCESS_TOKEN` : Wove API の認証に使う Supabase のアクセストークン（Bearer）

例:

```bash
WOVE_API_URL=http://localhost:8080
SUPABASE_ACCESS_TOKEN=your_access_token
```
